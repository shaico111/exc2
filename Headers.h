#pragma once
#pragma warning( disable : 6031)  // Because we can assume that the input is correct
#pragma warning( disable : 6011) // Because we can assume that there is enough memory
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define N 3